# Projeto Supabase corporativo (mesmo das demais micro-apps — SSO GoTrue)
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=

# Base da API do portal (vazio = mesma origem, via proxy /api)
VITE_API_URL=
